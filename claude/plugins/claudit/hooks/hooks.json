{
  "description": "claudit drift detector — reminds you to re-run /claudit when config has changed substantially or the last audit is stale",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-drift.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
